# https://github.com/uber-common/docker-ssh-agent-forward
if { ! type "pinata-ssh-forward" >/dev/null; } 2>&1; then
    echo "consider installing 'pinata-ssh-forward'"
    echo "[https://github.com/uber-common/docker-ssh-agent-forward]"
else
    pinata-ssh-forward >/dev/null 2>&1
fi
